services:
  vue-builder:
    build: .
    volumes:
      - ./dist:/app/dist-copy

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./dist:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    networks:
      - satsnet
    depends_on:
      - vue-builder

networks:
  backend-network:
    external: true
    name: satsnet